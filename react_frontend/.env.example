# Example environment variables for Create React App frontend
# Copy to .env and adjust as needed. All vars must be prefixed with REACT_APP_.

# Base URL to the FastAPI backend. Defaults to http://localhost:3001 if not set.
REACT_APP_API_BASE=http://localhost:3001

# Public site URL, used for redirects (e.g., auth flows) if needed.
REACT_APP_SITE_URL=http://localhost:3000
